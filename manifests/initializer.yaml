apiVersion: batch/v1
kind: Job
metadata:
  name: initializer
spec:
  template:
    spec:
      containers:
      - name: initializer
        image: initializer
      restartPolicy: Never
  backoffLimit: 4
